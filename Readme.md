Custom Chess Engine Project

This project contains a simple chess engine written in Python and a minimal web UI to play vs the engine.

Run:
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
./run.sh

Open web/index.html in a browser to try the demo.
